This a simple zsh shell script to easily setup mitmproxy for use with Claude Code. 

Script takes care of all http and certificate settings and handles all other corner cases.

Simply download the script, make it executable (chmod +x) and then run it. It automatically runs claude code.

After finishing, it automatically stops mitmproxy.
